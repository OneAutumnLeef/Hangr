# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build output
node_modules
dist
dist-ssr
*.local

# Vite
.vite
dev-dist

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env
.env
.env.local
.env.*.local

# Local AI / tooling state — Claude Code permissions, knowledge-graph caches,
# TS incremental build info. None of this is meaningful to other contributors.
.claude/
graphify-out/
*.tsbuildinfo

# PWA assets are committed so deployed builds always have the icons.
# Regenerate via `npm run generate-pwa-assets` whenever icon.png changes.
